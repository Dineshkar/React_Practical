import React from 'react'

function ListRenderingProps({person}) {
    return (
        <div>
        <h1>List Rendering using props {person.name}</h1>

        </div>
    )
}

export default ListRenderingProps
